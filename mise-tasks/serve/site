#!/usr/bin/env bash
#MISE description="Serve the Hugo site locally"
#MISE dir="{{config_root}}"
set -euo pipefail

hugo server --source site --bind 127.0.0.1 --baseURL http://127.0.0.1:1313/ --buildDrafts --disableFastRender
