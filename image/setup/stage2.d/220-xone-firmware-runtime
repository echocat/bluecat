#!/usr/bin/env bash
set -euo pipefail

# The proprietary Microsoft firmware is not shipped and not downloaded during
# the build. cabextract is only needed by /usr/bin/enable-xone-firmware after
# the user explicitly opts in at runtime.
echo "==> Installing cabextract (needed by enable-xone-firmware at runtime)"
rpm-ostree install -y cabextract
